eureka:
    client:
        service-url:
            defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}

server:
    port: 7979

hystrix:
    dashboard:
        proxy-stream-allow-list: "*"

# Logging
logging:
    level:
        root: INFO
        #        web: DEBUG
        com.tareqmy.hystrixserver: DEBUG
