spring:
    application:
        name: config-server
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/tareqmy/cloudconfigrepo
                    search-paths: configs
#  native mode cane applied with the following commented section
#                native:
#                    search-locations: classpath:/configurations
#    profiles:
#        active: native
server:
    port: 8888

